# امنیت

اگر آسیب‌پذیری امنیتی در پیشرو پیدا کردید، لطفاً آن را به‌صورت عمومی در Issues گزارش نکنید.

## گزارش مسئولانه

1. جزئیات فنی، مسیر بازتولید و شدت احتمالی را ایمیل کنید به: `info@pishrosarmaye.com`
2. تا زمان تأیید رفع، از افشای عمومی خودداری کنید.
3. در صورت امکان، نسخهٔ نرم‌افزار / commit و محیط (production یا local) را ذکر کنید.

## محدوده

مواردی مثل XSS، تزریق SQL، دور زدن احراز هویت ادمین/کاربر، افشای OTP یا دسترسی غیرمجاز به آپلودها در اولویت هستند.

## نکات استقرار

- هرگز secretهای production را commit نکنید.
- `NEXTAUTH_SECRET`، `ADMIN_JWT_SECRET` و `VIDEO_TOKEN_SECRET` باید تصادفی و قوی باشند.
- آپلودها را خارج از فولدر کد نگه دارید (`UPLOAD_BASE_DIR`).
- endpointهای دیباگ نباید در production در دسترس باشند.
