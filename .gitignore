# Node modules archives
node_modules.tar.gz
pishro-node-modules.tar.gz

# Large video files
public/videos/*.mp4

# Dependencies
node_modules/
.next/
dist/
prisma/engines/

# Environment variables
.env
.env.local
.env.*.local

# IDE
.vscode/
.idea/
*.code-workspace
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build outputs
.turbo/
*.tsbuildinfo

# Temporary/generated files
*.tmp
folders_tree.txt
tree-structure.txt
scripts/log.txt
uploads/tmp/

# Local backups and agent artifacts
.backup/
.hermes/
.next.backup-*/
.env.local.backup-*

# Generated uploads used for local seeding/tests
uploads/books/audio/
uploads/books/covers/
uploads/books/pdfs/

# Generated graphify artifacts
# Rebuild locally with `graphify update .` — the graph is derived from the
# source tree, so committing it only produces huge diffs on every code change.
graphify-out/

# Left in the deploy directory on the server by the old setup_parspack.sh.
# rclone.conf holds the ParsPack access/secret key in plain text, so this rule
# outlives the script that wrote it — the file is still sitting on the VPS.
rclone.conf
rclone_test_file.txt
npm-install.log
